# 调试模式说明

## 启动模式

OSS Browser 支持多种启动模式，满足不同的使用需求：

### 1. 开发模式 (npm run dev)
```bash
npm run dev
```

特点：
- 自动监听文件变化并重载
- 包含调试工具（可通过 F12 打开）
- 启动快速，无需完整构建
- 适用于日常使用和开发调试

### 2. 生产模式 (npm run prod)
```bash
npm run build && npm run prod
```

特点：
- 使用构建后的优化代码
- 不会自动打开调试工具
- 启动稍慢，但运行效率更高
- 适用于稳定环境使用

## 调试工具使用

### 手动打开调试工具的方式：

1. **快捷键**：按 F12 (Windows/Linux) 或 Cmd+Option+I (Mac)
2. **设置页面**：在设置对话框中点击"打开调试"按钮
3. **连续点击**：在应用左上角图标连续点击10次

### 调试信息查看：

- 网络请求：在 Network 标签页查看所有网络请求
- 控制台输出：在 Console 标签页查看日志信息
- 性能分析：在 Performance 标签页分析应用性能
- 元素检查：在 Elements 标签页检查和修改界面元素

## 环境变量

### NODE_ENV
- `development`：开发模式，启用调试功能
- `production`：生产模式，优化性能

### DEBUG
- 设置为 `'ali-oss'` 可以查看更多阿里云 OSS SDK 的调试信息
- 设置为 `'*'` 可以查看所有调试信息

## 最佳实践

1. **日常使用**：推荐使用 `npm run dev` 模式，便于查看错误信息
2. **性能测试**：使用 `npm run prod` 模式获得最佳性能
3. **问题排查**：使用调试工具定位和解决问题
4. **生产部署**：构建后使用生产模式运行，确保安全性和性能